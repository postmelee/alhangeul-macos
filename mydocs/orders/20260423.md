# 오늘 할일 - 2026년 4월 23일

## M010 — macOS Viewer 기반 (현재)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #3 | `ios/devel` native viewer core 변경을 `postmelee/rhwp` `devel`로 포팅하고 앱 저장소가 해당 core fork를 submodule로 추적하도록 전환 | 완료 | core 포팅 및 submodule 추적 전환 완료 |
| #5 | `rhwp-core.lock` commit SHA를 실제 `Vendor/rhwp` submodule HEAD로 보정 | 완료 | 문서/lock 보정, `git diff --check`와 submodule HEAD 대조로 검증 |
| #7 | `AGENTS.md` Codex 규칙 파일 작성 | 완료 | 문서 작업 완료, `git diff --check` 검증 |
| #9 | `README.md`를 현재 구현 상태와 장기 로드맵 기준으로 재작성 | 완료 | 문서 작업 완료, `git diff --check` 검증 |
| #11 | `AGENTS.md` 장문 운영 규칙을 manual 문서로 분리 | 완료 | manual 분리 완료, `git diff --check` 검증 |
| #15 | `AGENTS.md` Codex 표기 정리 및 `mydocs/orders/20260423.md` 형식 표준화 | 완료 | 계획서/단계 보고/최종 보고 작성 완료, 문서 작업 `git diff --check` 검증 |

## 검증 메모

- `cargo check --lib` (`Vendor/rhwp`)
- `./scripts/build-rust-macos.sh`
- `./scripts/check-no-appkit.sh`
- `xcodegen generate`
- `xcodebuild -project RhwpMac.xcodeproj -scheme HostApp -configuration Debug -derivedDataPath build/DerivedData CODE_SIGNING_ALLOWED=NO build`
- `./scripts/validate-stage3-render.sh`
- 문서 작업은 `git diff --check`로 검증한다.
