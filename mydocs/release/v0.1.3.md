# 알한글 v0.1.3 릴리즈 기록

## 상태

| 항목 | 값 |
|------|----|
| 상태 | `v0.1.3` build `9` local/rehearsal 검증 완료, Stage 5 승인 대기 |
| 목표 Git tag | `v0.1.3` |
| 목표 app version | `0.1.3` |
| 목표 build | `9` |
| 직전 public release | `v0.1.2` build `8` |
| GitHub Release | https://github.com/postmelee/alhangeul-macos/releases/tag/v0.1.3 |
| Pages 릴리즈 노트 | https://postmelee.github.io/alhangeul-macos/updates/v0.1.3.html |
| Public DMG | `alhangeul-macos-0.1.3.dmg` 예정 |
| Public DMG SHA256 | public workflow 결과 반영 예정 |
| Local rehearsal DMG | `build.noindex/release/alhangeul-macos-0.1.3-rehearsal.dmg` |
| Local rehearsal DMG SHA256 | `4aca645bc91b908844736cacb680acf9a08cd93f708435acb6721a24df5302aa` |
| Homebrew Cask | public DMG SHA256 확정 후 별도 반영 예정 |
| Release 실행 이슈 | [#267](https://github.com/postmelee/alhangeul-macos/issues/267) |

이 문서는 `rhwp v0.7.12` 반영과 `v0.1.3` public release 후보의 source metadata, release communication, 검증 예정 항목을 기록한다. public GitHub Release, signed/notarized DMG, stable Sparkle appcast, Pages 배포, Homebrew Cask 반영 결과는 release workflow와 별도 승인 단계가 끝난 뒤 같은 문서에 보강한다.

## 사용자용 요약

`v0.1.3`은 `rhwp v0.7.12` core/studio를 반영하는 patch release다. 업스트림의 HWP/HWPX parser, viewer, WMF/HWP3 처리, renderer 관련 수정이 알한글 앱의 WKWebView viewer asset과 Swift/Rust bridge 기반 Finder 통합 경로에 포함된다. 앱 본체, Quick Look preview extension, Finder thumbnail extension은 `0.1.3 (9)` 기준으로 맞춘다.

## 직전 공개 릴리즈 대비 변경점

기준 범위는 `v0.1.2..release-candidate`다. 최종 release tag의 peeled commit은 `main` release PR merge와 `v0.1.3` tag 생성 후 확정한다.

| 영역 | 변경 |
|------|------|
| Core/studio | #267: `rhwp` core와 bundled `rhwp-studio`를 `v0.7.12` release tag와 commit `1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5` 기준으로 갱신 |
| Release metadata | #267: 앱/extension source version을 `0.1.3`, build를 `9`, release workflow default와 helper dry-run 기준을 `v0.1.3`/`v0.7.12`로 정리 |
| Release communication | #267: README, Pages v0.1.3 릴리즈 노트, 내부 릴리즈 기록, third-party notices를 `rhwp v0.7.12` 기준으로 정리 |

## GitHub Release 본문 구조 후보

### 전체 요약

- `rhwp` core와 bundled `rhwp-studio`를 `v0.7.12` 기준으로 갱신했다.
- HWP/HWPX parser, viewer, WMF, HWP3, 렌더링 관련 upstream 수정이 앱과 Finder 통합 경로에 포함된다.
- 앱 본체와 Quick Look/Thumbnail extension의 release version은 `0.1.3 (9)`다.
- signed/notarized universal DMG, Sparkle stable appcast, Homebrew Cask 반영은 public workflow 결과를 확인한 뒤 확정한다.

### 포함된 rhwp 변화

- 포함된 `rhwp` core: [`v0.7.12`](https://github.com/edwardkim/rhwp/releases/tag/v0.7.12) (`1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5`)
- bundled `rhwp-studio`: [`v0.7.12`](https://github.com/edwardkim/rhwp/releases/tag/v0.7.12) (`1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5`)
- upstream release body는 비어 있다. tag message와 Stage 1 diff 기준으로 `@jangster77` 7-PR 시리즈, Issue #952 결함 수정, WMF #966, HWP3 #968, LTO #818 관련 변경을 사용자-facing 문장으로 정리한다.
- Rust FFI symbol set과 generated header hash는 이전 앱 릴리즈와 동일하므로 Swift bridge public ABI 변경은 없다.

### 알한글 앱 변화

- `rhwp-core.lock`, `RustBridge/Cargo.toml`, `RustBridge/Cargo.lock`, bundled `rhwp-studio/manifest.json`의 provenance가 모두 `v0.7.12` / `1899ef9...` 기준으로 일치한다.
- release rehearsal/publish workflow default는 `version=0.1.3`, `previous_release_ref=v0.1.2`, `expected_rhwp_tag=v0.7.12`다.
- Pages release note와 README는 `v0.1.3` 사용자-facing 안내를 준비하되, public DMG SHA256, Sparkle EdDSA signature, notarization 결과는 workflow 완료 후 기록한다.

## 연결된 Issue/PR

| Issue | PR | 상태 | 내용 |
|-------|----|------|------|
| [#267](https://github.com/postmelee/alhangeul-macos/issues/267) | 예정 | 진행중 | `rhwp v0.7.12` 반영, `v0.1.3` release candidate 준비와 public release handoff |

## 검증 기준

Stage 3 기준 source-level 검증:

| 영역 | 기준 | 상태 |
|------|------|------|
| App/extension version | HostApp, Preview, Thumbnail 모두 `0.1.3 (9)` | Stage 3에서 확인 |
| Core lock | `rhwp_release_tag=v0.7.12`, commit `1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5` | Stage 2에서 확인 |
| Studio asset | bundled manifest `source_release_tag=v0.7.12`, `source_resolved_commit=1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5` | Stage 2에서 확인 |
| Release helper | `scripts/ci/write-release-notes.sh 0.1.3 ...` template check | Stage 3에서 확인 |
| Pages release note | `docs/updates/v0.1.3.html` | Stage 3에서 추가 |

Stage 4 local/rehearsal 검증 결과:

| 영역 | 결과 | 비고 |
|------|------|------|
| Rust bridge lock | 통과 | `./scripts/build-rust-macos.sh --verify-lock` |
| Shared Swift boundary | 통과 | `./scripts/check-no-appkit.sh` |
| bundled studio asset | 통과 | `scripts/verify-rhwp-studio-assets.sh --tag v0.7.12 --commit 1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5` |
| Xcode project generation | 통과 | `xcodegen generate`; tracked project diff 없음 |
| Debug build | 통과 | `CODE_SIGNING_ALLOWED=NO`; Sparkle fetch 때문에 escalated retry 필요 |
| Release build | 통과 | 일반 My Mac destination 산출물은 `arm64` 단일 slice |
| DerivedData Release universal check | 실패/분리 | 일반 `xcodebuild` My Mac build 특성상 `Alhangeul` 실행 파일이 `arm64`만 포함 |
| Rehearsal release script | 통과 | `./scripts/release.sh --skip-notarize 0.1.3`; `generic/platform=macOS`, `ARCHS="arm64 x86_64"` |
| Rehearsal app universal check | 통과 | app, Quick Look extension, Thumbnail extension 모두 `x86_64 arm64` |
| Rehearsal DMG verify | 통과 | `hdiutil verify build.noindex/release/alhangeul-macos-0.1.3-rehearsal.dmg` |
| Rehearsal app version | 통과 | app/Preview/Thumbnail 모두 `0.1.3 (9)` |
| Native renderer smoke | 통과 | `validate-stage3-render.sh` 링크 플래그 보정 후 `KTX.hwp`, `request.hwp`, `exam_kor.hwp` 통과 |
| Release helper dry-run | 통과 | release notes template check 통과; delta checklist는 Stage 5에서 최종 HEAD 기준 재실행 필요 |

Stage 4에서 발견한 보정:

- `rhwp v0.7.12` native-skia 정적 라이브러리는 standalone `swiftc` smoke에서 `libc++`, `libiconv`, `libz` 링크가 필요하다. Xcode target은 이미 해당 SDK 의존성을 가진다.
- `scripts/validate-stage3-render.sh`, `scripts/render-debug-compare.sh`, `scripts/compare-quicklook-pdf-renderers.sh`에 동일 링크 플래그를 추가했다.

Stage 5 이후 보강할 검증:

- signed/notarized public DMG, GitHub Release asset, stable Sparkle appcast, Pages deploy 확인
- public installed app 기준 Finder Quick Look/Thumbnail smoke
- About 창의 `rhwp v0.7.12 (1899ef9)` 표시 확인
- public DMG SHA256, Sparkle EdDSA signature, Homebrew Cask digest 기록

## Release metadata

| 대상 | 기준 |
|------|------|
| App version | `v0.1.3` |
| `CFBundleShortVersionString` | `0.1.3` |
| `CFBundleVersion` | `9` |
| `rhwp` core repository | `https://github.com/edwardkim/rhwp.git` |
| rhwp core release tag | `v0.7.12` |
| rhwp core commit | `1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5` |
| bundled rhwp-studio release tag | `v0.7.12` |
| bundled rhwp-studio commit | `1899ef9bc2dfd1c6c0c4d18b192d253a2d0a1fb5` |
| core lock | `rhwp-core.lock` |
| studio manifest | `Sources/HostApp/Resources/rhwp-studio/manifest.json` |
| Third Party notices | `THIRD_PARTY_LICENSES.md`, `Sources/HostApp/Resources/Legal/THIRD_PARTY_LICENSES.md`, `Sources/HostApp/Resources/rhwp-studio/fonts/FONTS.md` |

## Release execution gate

public 배포 전에 다음 조건을 모두 만족해야 한다.

- [ ] `local/task267` 변경을 `publish/task267` PR로 게시하고 `devel`에 merge
- [ ] `devel`의 release candidate commit을 `main`에 반영하는 release PR 승인과 merge
- [ ] `main`의 최종 release commit에 `v0.1.3` tag 생성
- [ ] signed/notarized Release package build와 설치본 smoke 통과
- [ ] GitHub Release body가 `scripts/ci/write-release-notes.sh` template 기준으로 생성됐는지 확인
- [ ] public DMG `alhangeul-macos-0.1.3.dmg` SHA256 기록
- [ ] stable Sparkle appcast의 `sparkle:shortVersionString=0.1.3`, `sparkle:version=9`, EdDSA signature 확인
- [ ] public Pages `updates/v0.1.3.html`와 `appcast.xml` URL 확인
- [ ] Homebrew Cask를 public DMG SHA256 기준으로 갱신하고 tap context 검증 후 사용자 설치 명령 공개

## 알려진 제한 사항과 후속 이슈

- Quick Look preview와 Finder thumbnail은 native renderer 경로를 사용하고, 앱 viewer/editor는 WKWebView 기반 `rhwp-studio` 경로를 사용한다.
- 일부 문서에서는 글꼴, 배치, 페이지 표현이 원본 한글 프로그램과 다르게 보일 수 있다.
- HWPX 문서는 현재 직접 저장이 제한되어 HWP export 경로를 사용한다.
- public DMG SHA256, appcast EdDSA signature, notarization result, Homebrew Cask digest는 Stage 3 source commit에 미리 단정하지 않는다.

## Release communication checklist

- [x] `CFBundleShortVersionString`을 `0.1.3`으로 증가
- [x] `CFBundleVersion`을 직전 public build `8`보다 큰 `9`로 증가
- [x] release rehearsal/publish workflow default version을 `0.1.3`으로 정리
- [x] release workflow `previous_release_ref`를 `v0.1.2`로 정리
- [x] release workflow `expected_rhwp_tag`를 `v0.7.12`로 정리
- [x] README release 요약 갱신
- [x] `docs/updates/v0.1.3.html` 추가
- [x] `docs/updates/index.html`에 v0.1.3 항목 추가
- [x] third-party notices의 bundled `rhwp` provenance 갱신
- [x] local/rehearsal DMG 생성과 `hdiutil verify` 확인
- [x] local/rehearsal app/preview/thumbnail universal slice 확인
- [ ] installed candidate 기준 Finder Quick Look/Thumbnail smoke 확인
- [ ] About 창의 `rhwp v0.7.12 (1899ef9)` 표시 확인
- [ ] public DMG 생성
- [ ] Homebrew Cask `postmelee/tap/alhangeul` 공개와 install/uninstall smoke 확인
- [ ] public DMG 안의 app/extension version이 `0.1.3 (9)`인지 확인
- [ ] public DMG SHA256 기록
- [ ] public GitHub Release URL과 latest 상태 확인
- [ ] public `https://postmelee.github.io/alhangeul-macos/appcast.xml` stable item과 Sparkle EdDSA signature 확인
