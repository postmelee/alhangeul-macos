# Issue #11 단계 5 완료 보고서

## 수행 내용

- `AGENTS.md`의 하이퍼-워터폴 규율 문구를 원본 수준으로 강화했다.
- 신규 문서 파일명 규칙을 `task_{milestone}_{이슈번호}` 형식으로 고정했다.
- 외부 기여자 PR 처리 절차를 `AGENTS.md` 본문에서 바로 실행 가능한 수준으로 보강했다.
- `Codex` 단독 표기를 `Claude / Codex` 또는 `Claude와 Codex` 기준으로 정리했다.

## 판단

현재 저장소는 manual 분리 구조를 유지하는 것이 맞지만, 하이퍼-워터폴 핵심 규율과 외부 PR 처리 절차는 `AGENTS.md`에서 즉시 보일 필요가 있다. 특히 파일명 규칙과 승인 게이트는 에이전트가 작업 시작 시 바로 강하게 인식해야 실수율이 줄어든다.

## 결과

`AGENTS.md`는 프로젝트 맞춤형 구조를 유지하면서도, 원본 `CLAUDE.md`에 가까운 강한 통제력을 회복했다.
