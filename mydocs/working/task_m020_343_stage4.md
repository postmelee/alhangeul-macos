# Task M020 #343 Stage 4 — 실제 검색 검증 후 지원 문구 정렬

## 변경

#342 Stage 4~5를 merge로 가져오고 README·미공개 v0.2.0 초안·최종 보고서의 오래된 검색 미검증 표현을 갱신했다. 개발 후보에서 실제 HWP/HWPX 본문 검색·수정/보호/삭제 전파와 Spotlight 화면을 확인한 결과를 반영했다. 최초 설치 importer 발견 실패와 최소 OS/Intel runtime·공개 서명/공증/Sparkle은 남은 관문으로 명시한다.

기존 기본 연결 앱에서 검색 결과 문서를 연 것을 알한글 앱의 문서 열기 검증으로 바꾸어 표현하지 않았다. 실제 화면은 #342 증거에 연결하고 새로운 합성 이미지는 만들지 않았다.

## 검증

변경 문서의 상대 파일 링크 52개, `git diff --check`, 선행 head 대비 Sources·project.yml·core/Cargo lock·공개 docs 자산 불변을 확인했다. 모두 PASS. 이 단계는 문서 정렬이며 앱 재빌드·시스템 등록을 수행하지 않는다. 선행 #342의 실제 검색 결과는 해당 Stage 5와 결과 JSON을 따른다.

## 인계

문서 정렬 완료가 #342 최초 설치 해결이나 v0.2.0 출시 승인을 의미하지 않는다. PR #512는 사용자 리뷰를 기다리며 목표 버전·build·candidate·tag·공개 자산은 확정하지 않는다.
