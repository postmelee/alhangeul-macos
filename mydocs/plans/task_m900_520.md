# Task M900 #520 수행계획서

## 목적과 승인

2026-09-10 작업지시자의 진행 지시에 따라 v0.2.0 준비·동일 DMG 공개 절차 보완을 진행한다. 이슈 #520, 기능 Epic #337, 최초 설치 수용 #513, 자동화 #518 을 연결한다. Release Operations / M900, local/task520 → publish/task520 → devel.

기준 devel 41efb17, 직전 공개 v0.1.11 (17), 목표 v0.2.0 (18), core/Studio v0.8.6 유지. PR #517 저장/Word·HTML 수정까지 포함한다. #462 는 미병합으로 제외한다. 최종 candidate SHA는 준비 PR 리뷰·병합 후 확정한다.

## 범위

준비 PR에서 버전·포함 PR 분석·공개 문구 후보를 정렬하고, draft 생성 → 새 VM 검증 → 동일 DMG 승격을 분리한다. 검증 workflow run/attempt와 양 아키텍처의 실제 결과를 검사하고, draft의 DMG bytes와 입력 hash가 같은 경우에만 공개한다. 기존 공개 파일을 덮어쓰지 않는다.

준비 이후 main/tag·공증 draft·실제 후보 설치/GUI 검증은 확정한 후보로 별도 실행한다. 공개 게시·Sparkle/Pages 변경은 최종 검증 결과를 제시한 뒤 공개 승인을 받아 실행한다. Homebrew는 별도 범위다.

## 완료와 기록

준비 PR 완료와 전체 릴리스 완료를 구분한다. #520 은 실제 배포 종료까지 유지하며 #513 / #337 은 수용 기준을 충족한 뒤 종료한다. 공개 후보의 오류가 발견되면 같은 tag의 조용한 덮어쓰기 대신 원인·새 후보·기존 증거 무효화를 명시한다.
