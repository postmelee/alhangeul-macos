# Task M900 #535 수행계획서

2026-09-14 작업지시자의 진행 지시에 따라 #535 보완을 수행한다. Release Operations / M900, local/task535 → publish/task535 → devel, 기준 f12fb8df22e46a7393e0fb2b1b962db04963801b. 이 스레드의 보완·연속 진행 승인 범위 안에서 계획·단계 검증·보고·PR까지 진행한다. 공개 배포 승인은 별개다.

v0.2.1(19) 후보의 양 VM 실행 34726622024 attempt 1은 최초 설치·종료 후 검색을 통과했지만, 앱 제거 후 재준비한 문서가 이미 검색되어 재설치 전에 멈췄다. 삭제된 앱의 등록 해제 누락과 최초 오류 덮어쓰기까지 보완한다. 기존 실패 증거를 보존하고 제품의 재설치 회귀로 단정하지 않는다.

검색 유지/복구를 구분하되 같은 DMG·경로·빌드·mtime, 기존 요청 기록, 새 설치 객체·요청 식별자, 두 번째 실행 및 실행/종료 후 검색은 계속 강제한다. 최초 설치 미검색/TXT 대조와 lifecycle·정리를 완화하지 않는다. 부분 검색·조회 오류는 성공 증거로 쓰지 않는다.

불변 후보를 재빌드하거나 태그를 이동하지 않고, 검토되어 main에 반영된 검증 도구만 별도 SHA로 실행할 수 있도록 출처 계약을 보완한다. 후보와 도구 사이의 ancestry·허용 경로·bundle 버전, 실제 workflow run과 artifact/result의 도구 SHA를 검증한다. 제품·fixture·의존성 변경은 허용하지 않는다. 새 VM 실행은 도구 PR 검토·main 반영 뒤에 수행하고 실제 Mac/Sparkle 수용은 #532 에서 계속한다.
