# Task M020 #470 수행계획서

## 목적과 범위

known RenderNodeType의 손상된 payload와 unknown future variant를 구분한다. known key가 있으면 decode 오류를 전파하고, 올바른 externally tagged 형식의 unknown string/object variant는 `.unknown`으로 수용한다. 빈 object, 다중 variant object, known payload를 unit string으로 준 경우는 실패한다.

기존 `renderPageTree(at:) -> RenderNode?`는 유지하고 진단용 throwing API를 추가한다. 진단에는 known variant, schema coding path, DecodingError 분류를 담고 raw JSON·문서 텍스트·경로·underlying debugDescription은 기록하지 않는다. 제품 호출은 로그를 출력하지 않는다.

#394 PR #503 head `de4d374`를 상속한다. 작업지시자가 승인한 연속 작업 범위이며 추가 단계 승인 없이 구현·검증·PR까지 진행한다. merge와 golden producer #469 구현은 제외한다.

## 단계와 검증

1. strict known decoder 및 구조화된 실패 모델, minimal fixture 보강.
2. optional 호환 API/throwing API와 native smoke 진단 연결, bridge 운영 문서 정리.
3. decoder fixture·기본 3문서와 HWPX native smoke·HostApp build·no-AppKit·diff 검증 후 최종 보고/별도 PR.

core pin·Rust ABI·renderer layout은 변경하지 않는다. 하위 payload 내부의 별도 enum forward compatibility 정책 전체를 재설계하지 않는다.
